# kubectl get svc ingress-nginx-controller -n ingress-nginx

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-apps-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: abea0fad7a75c4f76abf07a400f6cf54-1742555946.eu-west-3.elb.amazonaws.com #""" kubectl get svc ingress-nginx-controller -n ingress-nginx """
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-weather-app
                port:
                  number: {{ .Values.weatherApp.service.port }}
#          - path: /
#            pathType: Prefix
#            backend:
#              service:
#                name: {{ .Release.Name }}-solitaire
#                port:
#                  number: {{ .Values.solitaire.service.port }}
